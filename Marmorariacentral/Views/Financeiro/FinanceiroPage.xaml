<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:models="clr-namespace:Marmorariacentral.Models"
             xmlns:viewmodels="clr-namespace:Marmorariacentral.ViewModels"
             x:Class="Marmorariacentral.Views.Financeiro.FinanceiroPage"
             x:DataType="viewmodels:FinanceiroViewModel"
             Title="Gestão Financeira"
             BackgroundColor="#F2F2F7">

    <Grid RowDefinitions="Auto, Auto, *">
        
        <Border Grid.Row="0" Padding="20" BackgroundColor="White" StrokeThickness="0">
            <Label Text="Contas e Fluxo" FontSize="24" FontAttributes="Bold" TextColor="#333"/>
        </Border>

        <FlexLayout Grid.Row="1" JustifyContent="SpaceAround" Padding="10" BackgroundColor="White">
            <Button Text="Contas" FontAttributes="Bold" BackgroundColor="Transparent" 
                    TextColor="#8B1A1A" Clicked="OnTabClicked" CommandParameter="Contas"/>
            <Button Text="Extrato" BackgroundColor="Transparent" 
                    TextColor="#777" Clicked="OnTabClicked" CommandParameter="Extrato"/>
            <Button Text="Produção" BackgroundColor="Transparent" 
                    TextColor="#CCC" Clicked="OnTabClicked" CommandParameter="Producao"/>
        </FlexLayout>

        <ScrollView x:Name="ViewContas" Grid.Row="2" Padding="15" IsVisible="True">
            <VerticalStackLayout Spacing="15">
                <Button Text="+ Cadastrar Conta" BackgroundColor="#8B1A1A" 
                        TextColor="White" CornerRadius="10" HeightRequest="50"
                        Command="{Binding AbrirCadastroCommand}"/>
                
                <Label Text="Contas Ativas" FontAttributes="Bold" Margin="5,0"/>

                <CollectionView ItemsSource="{Binding ContasAtivas}">
                    <CollectionView.ItemTemplate>
                        <DataTemplate x:DataType="models:FinanceiroRegistro">
                            <Border Margin="0,0,0,10" Padding="15" StrokeShape="RoundRectangle 12" BackgroundColor="White">
                                <Grid ColumnDefinitions="Auto, *, Auto">
                                    <Ellipse Grid.Column="0" WidthRequest="15" HeightRequest="15" 
                                             Fill="{Binding StatusColor}" VerticalOptions="Center" Margin="0,0,15,0"/>
                                    
                                    <VerticalStackLayout Grid.Column="1">
                                        <Label Text="{Binding Descricao}" FontAttributes="Bold"/>
                                        <Label Text="{Binding DataVencimento, StringFormat='Vence em: {0:dd/MM}'}" FontSize="12" TextColor="#777"/>
                                    </VerticalStackLayout>

                                    <HorizontalStackLayout Grid.Column="2" Spacing="10">
                                        <Label Text="{Binding Valor, StringFormat='{0:C}'}" VerticalOptions="Center" FontAttributes="Bold"/>
                                        <CheckBox IsChecked="{Binding FoiPago}" 
                                                  CheckedChanged="OnPagoChanged" 
                                                  BindingContext="{Binding .}"/>
                                    </HorizontalStackLayout>
                                </Grid>
                            </Border>
                        </DataTemplate>
                    </CollectionView.ItemTemplate>
                </CollectionView>
            </VerticalStackLayout>
        </ScrollView>

        <ScrollView x:Name="ViewExtrato" Grid.Row="2" Padding="15" IsVisible="False">
            <VerticalStackLayout Spacing="15">
                <Label Text="Movimentação (Itens Pagos)" FontSize="18" FontAttributes="Bold"/>
                <CollectionView ItemsSource="{Binding ExtratoDia}">
                    <CollectionView.ItemTemplate>
                        <DataTemplate x:DataType="models:FinanceiroRegistro">
                            <Border Margin="0,0,0,10" Padding="15" StrokeShape="RoundRectangle 12" BackgroundColor="#E8F5E9">
                                <Grid ColumnDefinitions="*, Auto">
                                    <VerticalStackLayout Grid.Column="0">
                                        <Label Text="{Binding Descricao}" FontAttributes="Bold"/>
                                        <Label Text="Pago" FontSize="11" TextColor="Green"/>
                                    </VerticalStackLayout>
                                    <Label Grid.Column="1" Text="{Binding Valor, StringFormat='{0:C}'}" VerticalOptions="Center" TextColor="Green" FontAttributes="Bold"/>
                                </Grid>
                            </Border>
                        </DataTemplate>
                    </CollectionView.ItemTemplate>
                </CollectionView>
            </VerticalStackLayout>
        </ScrollView>

        <VerticalStackLayout x:Name="ViewProducao" Grid.Row="2" VerticalOptions="Center" HorizontalOptions="Center" IsVisible="False">
            <Label Text="Aba em Construção 🏗️" FontSize="20" TextColor="#999"/>
            <Label Text="Depende da integração com Produção" HorizontalOptions="Center" TextColor="#CCC"/>
        </VerticalStackLayout>

    </Grid>
</ContentPage>